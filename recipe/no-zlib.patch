--- configure	2022-11-04 16:51:34.000000000 +0000
+++ configure.2	2022-11-05 22:32:01.450166100 +0000
@@ -740,8 +740,6 @@
 PKG_CONFIG
 zliblibs
 zlibcflags
-have_zutil_FALSE
-have_zutil_TRUE
 distconfigdir
 redhat_distro_FALSE
 redhat_distro_TRUE
@@ -17367,84 +17365,6 @@
 
 
 
-found_zutil=no
-zlibcflags=
-zliblibs="-lz"
-have_libz=no
-{ $as_echo "$as_me:${as_lineno-$LINENO}: checking for inflate in -lz" >&5
-$as_echo_n "checking for inflate in -lz... " >&6; }
-if ${ac_cv_lib_z_inflate+:} false; then :
-  $as_echo_n "(cached) " >&6
-else
-  ac_check_lib_save_LIBS=$LIBS
-LIBS="-lz  $LIBS"
-cat confdefs.h - <<_ACEOF >conftest.$ac_ext
-/* end confdefs.h.  */
-
-/* Override any GCC internal prototype to avoid an error.
-   Use char because int might match the return type of a GCC
-   builtin and then its argument prototype would still apply.  */
-#ifdef __cplusplus
-extern "C"
-#endif
-char inflate ();
-int
-main ()
-{
-return inflate ();
-  ;
-  return 0;
-}
-_ACEOF
-if ac_fn_c_try_link "$LINENO"; then :
-  ac_cv_lib_z_inflate=yes
-else
-  ac_cv_lib_z_inflate=no
-fi
-rm -f core conftest.err conftest.$ac_objext \
-    conftest$ac_exeext conftest.$ac_ext
-LIBS=$ac_check_lib_save_LIBS
-fi
-{ $as_echo "$as_me:${as_lineno-$LINENO}: result: $ac_cv_lib_z_inflate" >&5
-$as_echo "$ac_cv_lib_z_inflate" >&6; }
-if test "x$ac_cv_lib_z_inflate" = xyes; then :
-  have_libz="yes"
-else
-  have_libz="no"
-fi
-
-libp=`echo $LD_LIBRARY_PATH | sed -e s,::,:,g -e 's,/lib\(64\)*\:, ,g' -e 's,/lib\(64\)*$,,g'`
-for p in $libp /usr/local /usr ; do
-if test -f $p/include/zutil.h ; then
-zlibcflags="-I$p/include"
-zliblibs="-L$p/lib -lz"
-found_zutil=yes
-break
-elif test -f $p/include/zlib.h ; then
-zlibcflags="-I$p/include"
-fi
-done
- if test "$found_zutil" = "yes"; then
-  have_zutil_TRUE=
-  have_zutil_FALSE='#'
-else
-  have_zutil_TRUE='#'
-  have_zutil_FALSE=
-fi
-
-
-
-if test $have_libz = "no" ; then
-as_fn_error $? "libz not found" "$LINENO" 5
-fi
-
-
-
-
-
-
-
-
 if test "x$ac_cv_env_PKG_CONFIG_set" != "xset"; then
 	if test -n "$ac_tool_prefix"; then
   # Extract the first word of "${ac_tool_prefix}pkg-config", so it can be a program name with args.
@@ -20964,10 +20884,6 @@
   as_fn_error $? "conditional \"redhat_distro\" was never defined.
 Usually this means the macro was only invoked conditionally." "$LINENO" 5
 fi
-if test -z "${have_zutil_TRUE}" && test -z "${have_zutil_FALSE}"; then
-  as_fn_error $? "conditional \"have_zutil\" was never defined.
-Usually this means the macro was only invoked conditionally." "$LINENO" 5
-fi
 if test -z "${require_framecpp_TRUE}" && test -z "${require_framecpp_FALSE}"; then
   as_fn_error $? "conditional \"require_framecpp\" was never defined.
 Usually this means the macro was only invoked conditionally." "$LINENO" 5
